# Scan and timeline integration TODO

スキャン、変換、音声言語化、時間軸反映を、ユーザーに何度も手動実行させる画面ではなく、Timeline が最新状態を保つための自動処理として見せるための作業リスト。

## 採用方針

- `docs/scan-timeline-integration-prototypes.html` の案C「自動処理センター型」を採用する。
- 画面名は `スキャン` のままにする。
- 画面上では、素材取り込み、変換、音声言語化、時間軸反映をまとめて「自動処理の状態」として見せる。
- サブ製品の詳細一覧は、必要なときだけ確認する補助導線として扱う。
- Timeline 本体はサブ製品 Docker を直接触らない。サブ製品操作は `cli.ps1` または既存ランチャー経由に限定する。

## TODO

- [x] `/timeline` と `/scan` を同じスキャン画面へ寄せる。
- [x] サイドバーの主導線を `ダッシュボード` と `スキャン` に整理する。
- [x] スキャン画面の主表示を「自動処理の状態」中心へ寄せる。
- [x] `確認用データ` を `分析用データ` へ言い換える。
- [x] `対象製品` を `反映済み製品` へ言い換える。
- [x] TimelineForVideo を素材確認枠へ追加し、CLI確認待ちとして表示する。
- [x] 音声言語化の一括ジョブが実際に進行中か、補助サーバーの状態で確認する。
- [x] Playwright でスキャン画面を確認し、スクリーンショットを保存する。
- [x] 旧「時間軸一覧」として使っていない表示や導線が残っていないか再確認する。
- [x] `分析用データ` のページングで、最終ページが確定できない場合は `N / M ページ` と表示しないようにする。
- [x] サブ製品一覧の初期表示を軽くするため、一覧ページのページサイズを 25 件へ統一する。
- [x] 音声一覧の `最後` ボタンが最終ページへ移動することを Playwright で確認する。
- [x] 自動処理の各ステップを `素材取り込み`、`言語化`、`時間軸反映` のように段階表示する。
- [x] TimelineForVideo の CLI が通った後、動画の取り込み状態を素材確認枠から開けるようにする。
- [x] スキャン画面の主操作を `スキャン` に統一する。
- [x] `スキャン` から時間軸再構築後に一括音声言語化を開始する。
- [x] 上部の `反映済みイベント` / `対象素材` / `反映済み製品` / `最新確認` カードを削除し、重複表示を減らす。
- [x] スキャン画面から `分析用データ` ブロックを削除する。
- [x] 音声言語化の周辺ヒント範囲を前後 1 日に変更する。
- [x] TimelineForVideo の `audioModelMode` を Timeline 側の設定画面と保存経路から削除する。
- [x] 製品管理の `ready` 表示を、実稼働中と誤解しない `未起動` として表示する。
- [x] 音声言語化の完了、失敗、未解決の扱いを画面文言として整理する。
- [x] 時系列再構築の途中ステージを `preparing`、`downloading`、`importing`、`sorting`、`publishing` として画面に出せるようにする。
- [x] 音声言語化の一括対象から `needs_review` を外し、通常の `スキャン` では未開始、古い結果、失敗だけを再処理する。
- [x] `smoke-web.ps1` を現在の導線に合わせ、削除済みの旧設定ページ・旧操作ログページを確認対象から外す。
- [x] Video の素材確認表示を、単なる JSON 要約ではなくユーザーが理解できる代表表現へ整理する。
- [x] `スキャン` の時間軸再構築前に、CLI 経由で各製品の refresh を走らせる。ChatGPT はユーザー指定ZIPが必要なため自動refresh対象外。
- [x] Video のような大きいZIPを取り込む時に止まって見えないよう、製品取り込み中の件数進捗をワーカー状態へ出せるようにする。
- [x] 一括音声言語化の次回対象件数を読み取り専用 API で確認できるようにし、画面上で再試行対象があることを示す。
- [x] スキャン画面の素材別確認を、左列 `音声 / 動画 / 画像`、右列 `ChatGPT / Windows Codex / PC状態` に整理する。
- [x] Video と PC状態の素材別確認カードをクリック可能にし、一覧ページへ遷移できるようにする。
- [x] Video の `files list` を補助サーバー経由で読み取り、動画ファイル一覧として表示する。
- [x] PC の `items list` を補助サーバー経由で読み取り、PC状態一覧として表示する。
- [x] 各一覧の主導線にパンくずを追加し、スキャンから補助一覧へ入っていることを明確にする。
- [x] 音声ファイル一覧から個別の `再スキャンして分析` ボタンを外し、スキャン画面へ役割を寄せる。
- [x] Video のファイル名をクリック可能にし、動画詳細ページで動画プレビューと分析件数を確認できるようにする。
- [x] 画像、Windows Codex、ChatGPT の詳細ページもパンくず導線へ寄せ、一覧に戻るボタンを削除する。
- [x] `smoke-web.ps1` に Video / PC の画面、Video 詳細、Video source、PC items の確認を追加する。
- [x] 言語化プロンプトから要約に寄る指示を外し、発話単位の文字化・翻訳候補を返す方針へ寄せる。
- [x] 巨大な時系列ストアの全件ソートを、PowerShell のメモリソートではない方式へ再設計する。
- [x] `スキャン` 実行後、音声言語化が完了して成果がある場合は、自動で時間軸を再反映する。失敗で止まった場合は勝手に進めず、エラー・再試行対象の表示に留める。
- [x] 製品管理の実稼働状態を、Timeline から起動した履歴だけで見るか、各製品の CLI/overview で実状態を確認するかを決める。サブ製品Dockerを直接確認せず、CLIの状態確認が副作用でworkerを起動する製品もあるため、製品管理では `Timeline から操作した起動状態` と `CLI接続可能状態` を分けて扱う。
- [x] 設定と製品管理は通常ページへ戻し、破壊的操作など重要判断だけを確認モーダルとして表示する。
- [x] Video 詳細画面で動画そのものを再生し、言語化済みの音声区間があれば読める候補を表示する。
- [x] 言語化プロンプト修正後の既存結果を、いつ再生成するか決める。プロンプト版を `audio-verbalization-v2` に上げ、既存結果は次回スキャン時に `stale` として再言語化対象へ戻す。
- [x] Video の `audio_acoustic_units` 由来の `phone_tokens` を一括言語化対象に含める。Video の音声区間は Timeline store から発話単位に組み立て、Audio と同じ言語化保存先で管理する。
- [x] PC 状態の詳細画面を作るか決める。現時点では `items list` の一覧確認を主導線とし、詳細画面は作らない。TimelineForPC は単発のWindowsホスト記録で、まずは一覧で取り込み可否を確認できれば足りるため、詳細は必要になった時に `timeline.json` / `convert_info.json` / `report.md` を読むAPIとして追加する。
- [x] 音声詳細画面で通常ユーザー向けにフォントークン原文を出さず、言語化結果と音声再生を中心に確認できるようにする。
- [x] 音声再生位置に合わせて言語化結果の行を強調できるようにする。
- [x] Video 一覧の生成物列がサイズ列と重ならないよう、Video 用の列幅と折り返しを調整する。
- [x] 一括言語化の進捗計算を、完了ファイル数だけでなく処理済み区間数も使うようにする。長い動画の途中処理でも次回起動分から 0% 固定になりにくくする。
- [x] `/scan` の初期表示とポーリングで、重い一括言語化対象の再集計を待たないようにする。状態表示を先に出し、対象件数は必要なタイミングで後から取得する。
- [x] Audio 一覧で、全件分の言語化状態を作ってからページングしないようにする。ページング後の表示対象だけ言語化状態を付け、初期表示の負荷を下げる。
- [x] Video / Image / PC 一覧で、一覧本体の取得を待つ前に概要カードを表示する。重い一覧APIが残っていても画面全体が空白のまま待たないようにする。
- [x] スキャン集約方針に合わせ、Windows Codex と Image の一覧ページから個別のスキャン実行ボタンを外す。ChatGPT の ZIPアップロードはユーザー指定ファイルが必要なため残す。
- [x] 一覧ページで一度に見える件数を増やすため、ファイルツリー行、表行、一覧アクションバーの縦密度を少し詰める。
- [x] 画像ファイル一覧を、音声・動画と同じくディレクトリ階層付きのファイルツリー表示へ寄せる。
- [x] 音声、動画、画像、Windows Codex、ChatGPT、PC の一覧セクション見出しを `アイテム一覧` に統一する。
- [x] 一覧カード内のスクロール領域を、画面下までの残り高さへ追従させ、ページ全体に不要な縦スクロールが出にくい構造へ揃える。
- [x] 各一覧ページの上部カードを、総数だけ・状態だけのカードではなく、処理済み / 対象の比率で判断できる最小項目へ整理する。
- [x] 音声・動画の言語化件数は、一部区間ではなくファイル全体が言語化済みになったものだけを完了として数える。
- [x] 音声・動画の言語化件数を、表示中ページではなく全体の対象ファイル数 / 完了ファイル数で表示する。
- [x] 言語化プロンプトへ表示言語を明示し、`ja-JP` ではローマ字だけ・カタカナだけに寄らないよう指示を強める。
- [x] スキャン画面の自動処理状態で、上部と下部で重複している項目数・イベント数表示を削除し、段階カード側へ寄せる。
- [x] 製品管理で、稼働中は起動不可、停止中は停止・再起動不可になるよう、状態別の操作可否を確認する。
- [x] Timeline を再起動して、設定ページと製品管理ページが常駐モーダルなしで表示されることを実画面で確認する。
- [x] 製品管理のアンインストール確認は、製品行内の展開ではなく確認モーダルで表示する。
- [x] 設定ページは Timeline 本体設定を先に表示し、Audio / Image / Video の重い設定確認は後追いで読み込む。
- [x] Video 一覧は、重い overview を待ってから一覧を取る直列処理をやめ、先にファイル一覧を表示して overview は後追い更新する。
- [x] 既存の要約寄り言語化結果を、どのタイミングで新プロンプトの文字化・翻訳候補として再生成するか決める。次回スキャン時に `stale` 判定で再処理する。
- [x] Video の `files list` / overview に短期キャッシュを追加し、通常の再表示では待ち時間を減らす。ユーザーが `一覧更新` を押した場合は `refresh=1` で CLI 読み直しを行う。
- [x] Video の `files list` / overview に作業ディレクトリ配下の永続キャッシュを追加し、helper 再起動後の通常表示でも直近成功結果をすぐ返せるようにする。
- [x] 一括言語化の実行中は `/timeline/audio-verbalization/bulk/targets` で全件再集計せず、実行中ステータスから軽量な対象サマリーを返す。
- [x] Video / Image / PC 一覧は、初期描画前に一覧取得を待たず、先に読み込み状態を表示してから一覧取得する。
- [x] PC 状態一覧で、読み込み中に `PC状態はありません。` と誤表示されないようにする。
- [x] 非実行中の `/timeline/audio-verbalization/bulk/targets` に短期キャッシュを追加し、ページ遷移や状態更新のたびに Audio / Video の対象件数を全再集計しないようにする。
- [x] Audio overview に短期キャッシュを追加し、ダッシュボード、スキャン、音声一覧、設定モーダルで同じ概要情報を毎回再集計しないようにする。
- [x] ダッシュボードの `取り込み済み素材` と `素材別の状況` を Timeline store の反映済み件数へ寄せ、スキャン画面の `素材取り込み` と件数定義を揃える。
- [x] 画像ファイル一覧の日本語ファイル名表示を実画面で確認し、smoke の文字化け検査が日本語ファイル名まで届くようにする。
- [x] PC状態一覧で `first_seen` などのCLI内部ステータスをそのまま表示せず、ユーザー向けの日本語ラベルへ変換する。

## 判断待ち・保留

- Video の `files list` 自体は、2026-05-10 時点で CLI 経由の読み取りに約 13 秒かかる。画面側は先に一覧を出す構造へ寄せたが、さらに速くするには TimelineForVideo 側の `files list` 高速化か、Timeline helper 側の永続キャッシュ設計が必要。
- Video の永続キャッシュは 1 時間 TTL。`一覧更新` やスキャン実行では明示的に CLI 読み直しを行う。TTL 切れ後の初回取得は引き続き TimelineForVideo CLI の速度に依存する。
- 製品管理ページは、`/scan` 初期ロード中など helper が重い処理中の場合に数秒以上 `読み込み中` になることがある。UI上は待機表示できているが、根本改善するなら helper の並列処理または起動状態キャッシュが必要。
- 2026-05-10 の言語化 v5 検証では、音声1件・動画1件とも読める候補を作れなかった。誤候補を出さない安全弁は入ったが、品質改善までは全件処理へ戻さない。
- `/timeline/audio-verbalization/bulk/targets` の短期キャッシュは 15 分 TTL。スキャン直後や状態変化後は無効化されるが、TTL の長さは言語化の全件処理を再開する段階で再調整する余地がある。
- TimelineForImage は製品側生成済み 705 件、Timeline store 反映済み 704 件という差がある。差分は `image-023099e810527f0c` で、成果物側には `timeline.json` と `image_record.json` が両方存在する。成果物更新は 2026-05-09 23:46:54、Timeline store 更新は 2026-05-09 14:16:07 なので、現時点では store 再構築後に生成された未反映データとして扱う。ダッシュボードでは Timeline として使えるデータを示すため、store 反映済み件数を表示する。
- 2026-05-10 のスクリーンショット確認では、Windows Codex / ChatGPT / PC の一覧ページは cold load 直後 8 秒時点で `読み込み中` の場合があり、30 秒時点では表示される。読み込み状態は出ているが、さらに詰めるなら一覧 API のキャッシュまたは先読みが必要。

## 現在確認した状態

- 一括音声言語化は `audio-verbalization-bulk-20260509-005814-f271c650` が完了。
- 完了結果は対象 66 件、未解決 53 件、失敗 13 件。失敗理由はログ上 `Ollama request failed...`。
- `needs_review` は有力候補が作られた状態として扱い、通常の `スキャン` では再言語化しない。
- スクリーンショット: `output/playwright/scan-after-c-redesign-step.png`
- スクリーンショット: `output/playwright/scan-analysis-pagination-after-partial-label.png`
- スクリーンショット: `output/playwright/audio-pagination-last-check-after-wait.png`
- スクリーンショット: `output/playwright/scan-step-status-after-implementation.png`
- 2026-05-11 に設定と製品管理を常駐モーダルから通常ページへ戻した。`/timeline/settings` と `/timeline/products` は `role=dialog` なしで表示され、アンインストール確認だけがモーダルとして表示される。スクリーンショット: `output/playwright/timeline-settings-page-final-clean.png`, `output/playwright/timeline-products-page-final-clean.png`, `output/playwright/product-uninstall-confirm-modal-final-clean.png`
- 2026-05-09 に TimelineForVideo を含む時系列再構築が完了。
- 再構築結果は 4,417 アイテム / 750,843 イベント。
- Video は 234 アイテム / 614,202 イベントとして反映済み。
- Video の通常イベントは `video_event_summary` として `Video source observed: ...` や `Frame sample; ...` のような代表表現で保存される。OCR済みテキストは `text` のまま保存する。
- PC は認識済みだが production item が 0 件のため、現時点では時系列ストアに含まれていない。
- 2026-05-09 の画面確認では、音声言語化は失敗 13 件として表示されている。旧 `未言語化をすべて言語化` ボタンと `分析用データ` ブロックは表示されていない。
- スクリーンショット: `output/playwright/scan-after-status-wording-and-stages.png`
- `needs_review` を一括再処理対象から外した後、次回一括対象は失敗 13 件だけになることを確認。
- スクリーンショット: `output/playwright/scan-after-needs-review-target-fix.png`
- 読み取り専用の `/timeline/audio-verbalization/bulk/targets` で、次回一括言語化の対象件数を確認できるようにした。
- スクリーンショット: `output/playwright/scan-after-bulk-target-summary-loaded.png`
- 2026-05-09 に失敗 13 件を一括再実行した。ジョブ `audio-verbalization-bulk-20260509-134942-1a86bbd8` は完了し、13 件すべてが未解決へ移行、失敗 0 件、次回一括対象 0 件になった。
- 完了結果は対象 13 件、未解決 13 件、失敗 0 件、86 / 86 チャンク、言語化済み turn 305 件、未解決 turn 611 件、処理時間 3004 秒。
- スクリーンショット: `output/playwright/scan-after-audio-verbalization-retry-completed.png`
- 2026-05-09 に Video 一覧と PC 一覧の API を確認。Video は 234 件、PC は 1 件を返す。
- Video 詳細 API は `C:\Users\amano\Videos\2026-04-23 16-59-46.mp4` で詳細取得に成功。`/api/video/source` は Range 付き GET で `206 Partial Content` / `video/mp4` を返すことを確認。
- 2026-05-09 に `scripts/smoke-web.ps1` へ Video / PC / Video source の確認を追加し、smoke が通ることを確認。
- 2026-05-09 に言語化プロンプトから要約指示を削除し、発話単位の文字化・翻訳候補を返す方針へ修正。既存結果を再生成するかは未決定。
- 2026-05-09 に時系列イベントの外部ソートを追加。200MB 超の `events.jsonl` はチャンクソート後にマージし、メモリ全載せや未ソート return を避ける。小さな JSONL で外部ソート smoke を確認。
- 2026-05-09 に Video の `phone_tokens` の扱いを確認。Timeline store には Video の `audio_acoustic_units` が `phone_tokens` として入るが、一括言語化対象は `Get-TimelineAudioVerbalizationBulkTargets` 経由の Audio ファイルに限定されている。LLM 入力生成では未言語化の `phone_tokens` は除外されるため、直接読みにくいデータを LLM に渡さない方針は守られている。
- 2026-05-09 に Video の `phone_tokens` を一括言語化対象へ追加。初回の対象集計では Video 229 件が未言語化対象として検出された。Video の対象数は Timeline store の `events.jsonl` から軽量インデックスを作って数え、実行時だけ各 Video timeline から発話単位の詳細を組み立てる。
- 2026-05-09 に PC 状態一覧の連携範囲を確認。TimelineForPC は `items list` で一覧取得できるが、詳細取得 API はまだ Timeline 側にない。画面では内部 ID を常時表示しないようにした。
- 2026-05-09 に Video 画面の文言を実装状態へ合わせた。Video の音声区間は一括言語化対象に含め、詳細画面では動画プレビューと言語化結果を確認できるようにした。
- スクリーンショット: `output/playwright/scan-current.png`
- スクリーンショット: `output/playwright/video-current-final.png`
- 2026-05-09 に `/timeline/audio-verbalization/bulk/targets` で Audio / Video の一括言語化対象 229 件を検出できることを確認。
- 2026-05-09 に一括言語化ジョブ `audio-verbalization-bulk-20260509-230454-82a802f7` を起動。即終了せず `running` になり、対象 229 件 / 7,401 区間として処理開始した。23:06 時点で 1 チャンク完了、次チャンク処理中。
- 2026-05-09 23:41 時点で一括言語化ジョブ `audio-verbalization-bulk-20260509-230454-82a802f7` は継続中。対象 229 件、候補あり 11 件、言語化済み 174 区間、未解決 200 区間、進捗 5%。
- 2026-05-09 24:25 時点で一括言語化ジョブ `audio-verbalization-bulk-20260509-230454-82a802f7` は継続中。対象 229 件、候補あり 22 件、失敗 0 件、言語化済み 462 区間、未解決 734 区間、進捗 10%。
- 2026-05-09 に `/scan` の初期ロード遅延を確認。`/timeline/audio-verbalization/bulk/targets` が約 19 秒かかって画面全体をブロックしていたため、初期ロードと5秒ポーリングから分離した。
- 2026-05-09 に設定・製品管理モーダルを固定ヘッダー付きへ変更。右上の閉じるボタン、リサイズ、ヘッダードラッグ用の `timeline-modal.js` を追加。実行中の一括言語化ジョブを止めないため、画面反映確認は再起動後に行う。2026-05-11 に方針変更し、設定と製品管理は通常ページへ戻したため、この専用 JS は削除済み。
- 2026-05-09 に Audio 一覧の取得負荷を確認。1ページ表示でも全件分の言語化状態を作っていたため、表示ページ分だけ状態を付けるよう修正。実行中の一括言語化ジョブを止めないため、速度確認は再起動後に行う。
- 2026-05-09 に Video / Image / PC 一覧の初期表示を調整。概要取得後に一度画面を反映し、その後に一覧ページを読み込むため、一覧APIが遅くてもユーザーは対象件数や状態を先に確認できる。
- 2026-05-09 に Windows Codex と Image の一覧ページから個別スキャンボタンを削除。スキャンの主操作は `/scan` に集約する。ChatGPT はZIPアップロードが必要なため個別導線を維持。
- 2026-05-09 に一括言語化完了後の自動時間軸再反映を追加。`running` / `queued` / `starting` から `completed` に変わり、言語化済み区間または候補あり件数がある場合だけ再構築する。失敗状態では自動再構築しない。
- 2026-05-09 に Windows Codex と Image の一覧ページから、画面上の個別スキャンボタン削除後に残っていた個別スキャン用の内部進捗表示・未使用処理も削除。各一覧ページは確認、削除、ダウンロードに寄せる。
- 2026-05-09 にファイル一覧、スレッド一覧、画像一覧などの表示密度を少し上げた。ファイルツリー行、表行、一覧アクションバーの縦余白を詰め、同じ画面高さで見える件数を増やす。
- 2026-05-09 24:40 時点で一括言語化ジョブ `audio-verbalization-bulk-20260509-230454-82a802f7` は継続中。対象 229 件、候補あり 23 件、失敗 0 件、言語化済み 510 区間、未解決 775 区間、進捗 10%。
- 2026-05-10 に画像一覧をファイルツリー表示へ変更。音声、動画、画像、Windows Codex、ChatGPT、PC の補助一覧セクション名を `アイテム一覧` に統一した。
- 2026-05-10 に各補助一覧の上部カードを減らし、状態や表示中件数の重複カードを削除した。音声・動画の言語化済み件数は、表示対象ファイルのうち `completed` になったファイルだけを完了として数える。
- 2026-05-10 に一覧スクロール領域へ `tfa-list-card` / `tfa-list-scroll` を追加し、画面下までの残り高さに追従しやすい構造へ寄せた。
- 2026-05-10 に TimelineForAudio overview へ `audioItemCount` を追加し、音声一覧の取り込み済み件数を対象ファイル総数との比率で表示できるようにした。
- 2026-05-10 に Video 一覧行へ `audioVerbalization` 状態を付与し、動画ファイルの言語化済み件数をファイル単位で表示できるようにした。
- 2026-05-10 に Audio / Video overview へ全体の言語化対象ファイル数と言語化済みファイル数を追加。上部カードが表示中ページだけの件数に寄らないようにした。
- 2026-05-10 に言語化プロンプトへ `context.language` の扱いを明記し、`ja-JP` では自然な日本語を返し、ローマ字のみ・カタカナのみへ寄らないようにした。
- 2026-05-10 に言語化プロンプト版を `audio-verbalization-v2` へ更新。旧プロンプトで作られた要約寄り結果は、次回スキャン時に `stale` として再言語化対象へ戻る。
- 2026-05-10 に製品管理の状態表示方針を確定。サブ製品Dockerを直接見ず、Timeline が起動・停止した履歴を起動状態として扱う。CLIが見つかるだけの状態は稼働中ではないため、画面では `未起動` と表示する。
- 2026-05-10 に PC 状態の詳細画面は一旦作らない方針にした。TimelineForPC の現行連携は一覧で取り込み確認するところまでとし、詳細が必要になったら既存成果物を読む専用APIを追加する。
- 2026-05-10 に設定モーダルを再起動後に確認。固定ヘッダー、右上閉じる、固定フッターの保存ボタン、通常モードの先行表示を確認した。スクリーンショット: `output/playwright/verify-settings-6s.png`
- 2026-05-11 に製品管理モーダルを再起動後に確認。`ready` は `未起動` で、CLI が見つかり起動できる状態として扱う。起動ボタンは有効、停止・再起動は無効、PC は `起動停止不要` として表示される。スクリーンショット: `output/playwright/product-modal-state-label-after-helper-ok.png`
- 2026-05-10 に一覧カードの画面下追従を再確認。`.tfa-main` の下余白を計算に含め、一覧カードの高さ指定によるページ全体の余分な縦スクロールを抑制した。スクリーンショット: `output/playwright/verify-list-layout-after-bottom-gap-video.png`, `output/playwright/verify-list-layout-after-bottom-gap-image.png`, `output/playwright/verify-list-layout-after-bottom-gap-windows-codex.png`, `output/playwright/verify-list-layout-after-bottom-gap-chatgpt.png`, `output/playwright/verify-list-layout-after-bottom-gap-pc.png`
- 2026-05-10 に音声言語化の周辺ヒント範囲を、設定値が読めない場合でも前後 1 日へ揃えた。既定値やフォールバックが 10 分に戻らないよう、補助サーバー側の fallback を 1440 分へ統一した。
- 2026-05-10 に音声一覧の長め待機を確認。再起動直後や一括言語化中は数秒では `読み込み中` のままになることがあるが、30 秒待機では 79 件の一覧が表示された。スクリーンショット: `output/playwright/verify-audio-longwait-after-permission.png`
- 2026-05-10 に PowerShell ASCII guard、`dotnet.exe build web/Timeline.Web.csproj`、`scripts/smoke-web.ps1` を再実行し、いずれも成功した。
- 2026-05-10 にモーダル右下のリサイズ操作後に閉じてしまう問題を修正。背景クリックの閉じる処理をモーダル全体から専用バックドロップへ移し、リサイズハンドル側の click が閉じる処理へ届かないようにした。Playwright で設定モーダルと製品管理モーダルのリサイズ後も表示が残ることを確認。スクリーンショット: `output/playwright/verify-settings-modal-after-resize-stays-open.png`, `output/playwright/verify-products-modal-after-resize-stays-open.png`
- 2026-05-10 に設定モーダルと製品管理モーダルの Escape close を確認。製品管理は右上の閉じるボタンでも閉じることを Playwright で確認。スクリーンショット: `output/playwright/verify-settings-modal-escape-close.png`
- 2026-05-10 に画像一覧の初期表示を確認。1秒時点では `読み込み中`、8秒時点ではディレクトリ階層付き一覧が表示される。スクリーンショット: `output/playwright/verify-image-1s-loading.png`, `output/playwright/verify-image-8s-loaded.png`
- 2026-05-10 に Video 一覧の段階表示を確認。6秒時点ではページ全体の読み込み中、15秒時点でファイル一覧が表示され、25秒時点で overview の全体件数へ更新される。スクリーンショット: `output/playwright/verify-video-progressive-6s.png`, `output/playwright/verify-video-progressive-15s.png`, `output/playwright/verify-video-progressive-25s.png`
- 2026-05-10 に `scripts/smoke-web.ps1` を実行し、Webページ、主要 helper API、Audio/Image/Video/PC の確認が通ることを確認。`/timeline/audio-verbalization/bulk/targets` は約 19 秒かかるが、画面初期表示からは分離済み。
- 2026-05-10 に Video の `files list` / overview へ短期キャッシュを追加。再起動直後の実測では初回 `files list` が約 13.6 秒、2回目が約 0.18 秒。overview は初回約 10.7 秒、2回目が約 0.11 秒。キャッシュが効いている状態では `files list` 約 0.16〜0.30 秒、overview 約 0.13〜0.15 秒。
- 2026-05-10 に Video の `files list` / overview へ永続キャッシュを追加。別 PowerShell プロセスで実測し、`files list` は CLI 読み直し 14.357 秒、永続キャッシュ読み 0.147 秒。overview は CLI 読み直し 10.277 秒、永続キャッシュ読み 0.089 秒。`start.ps1 -NoOpen` 後の smoke では `/products/video/overview` が 0.129 秒で通過。
- 2026-05-10 に `/timeline/audio-verbalization/bulk/targets` を実行中だけ軽量化。実行中は全件再集計せず、`latest.json` の状態から残り件数を返す。smoke では 19.363 秒から 0.235 秒へ短縮。
- 2026-05-10 に非実行中の `/timeline/audio-verbalization/bulk/targets` へ短期キャッシュを追加。強制再計算は 15.814 秒、通常呼び出しは 0.039〜0.224 秒になった。初回は 120 秒 TTL で短すぎたため、15 分 TTL へ伸ばした。`scripts/smoke-web.ps1` では同 API が 0.109 秒で通過。
- 2026-05-10 に Audio overview へ短期キャッシュを追加。処理中はキャッシュせず通常時だけ使う。強制再計算は 3.486 秒、通常呼び出しは 0.067〜0.090 秒になった。`scripts/smoke-web.ps1` では同 API が 0.09 秒で通過。
- 2026-05-10 にダッシュボード件数の定義を確認。Image overview は製品側生成済み 705 件、Image files は現在入力 24 件、Timeline store は Image 704 件を返していたため、ダッシュボードは store の `itemCount` / product `itemCount` を使うように統一した。
- 2026-05-10 にダッシュボードとスキャンの件数表示を実画面確認。どちらも `取り込み済み素材` / `素材取り込み` が 4,419 件、Image が 704 件で揃うことを確認した。
- 2026-05-10 に画像ファイル名の文字化けを切り分け。PowerShell ターミナル表示では崩れることがあるが、API内部の文字列は正しい日本語コードポイントで、画面でも `スクリーンショット ...` と表示されることを確認した。smoke は先頭3件だけだと ASCII 名しか検査しないため、画像ファイル検査を 8 件へ広げた。
- 2026-05-10 に Image の 705 / 704 件差分を追加調査。成果物 `image-023099e810527f0c` は `timeline.json` と `image_record.json` が両方あり、生成時刻が Timeline store 更新後だったため、次回スキャンで反映される未反映データと判断した。コード修正は不要。
- 2026-05-10 に Video / Image / PC の初期表示を OnAfterRender 後の読み込みへ移し、初期DOM表示をブロックしないようにした。Playwright 実測では Video / Image / PC の初期DOM表示は 0.2 秒未満。
- 2026-05-10 に PC 状態一覧の読み込み中表示を修正。読み込み中の空表示をやめ、一覧取得後に最終更新表示へ切り替わることを確認。スクリーンショット: `output/playwright/verify-pc-loading-cleared-5s.png`
- 2026-05-10 にモーダルのリサイズドラッグ後、マウスボタンを離すと閉じる問題へ対応。バックドロップクリックで閉じない構造にし、閉じる操作は固定ヘッダーの閉じるボタンに寄せた。
- 2026-05-10 に PC 状態一覧の `first_seen` 表示を `初回取得` へ変換することを確認。スクリーンショット: `output/playwright/current-pc-after-status-label.png`
- 2026-05-10 にスキャン画面の現状を確認。スクリーンショット: `output/playwright/current-scan-after-pc-status-label.png`
- スクリーンショット: `output/playwright/verify-video-progressive-after-onafterrender-1s.png`
- スクリーンショット: `output/playwright/verify-video-progressive-after-onafterrender-15s.png`
- スクリーンショット: `output/playwright/verify-video-persistent-cache.png`
- スクリーンショット: `output/playwright/verify-scan-after-cache.png`
- スクリーンショット: `output/playwright/verify-scan-after-bulk-target-fastpath.png`
- スクリーンショット: `output/playwright/scan-15s.png`
- スクリーンショット: `output/playwright/audio-detail.png`
- スクリーンショット: `output/playwright/video-detail-15s.png`
- スクリーンショット: `output/playwright/video-list-fixed-2.png`
- スクリーンショット: `output/playwright/pc-list-15s.png`
- スクリーンショット: `output/playwright/verify-scan-after-verbalization-v5-wording.png`
- スクリーンショット: `output/playwright/verify-audio-list-after-verbalization-v5.png`
- スクリーンショット: `output/playwright/verify-scan-after-bulk-target-summary-cache.png`
- スクリーンショット: `output/playwright/verify-scan-after-audio-overview-cache.png`
- スクリーンショット: `output/playwright/verify-dashboard-after-audio-overview-cache.png`
- スクリーンショット: `output/playwright/verify-dashboard-store-count-unified.png`
- スクリーンショット: `output/playwright/verify-scan-store-count-reference.png`
- スクリーンショット: `output/playwright/verify-image-encoding-current.png`
- スクリーンショット: `output/playwright/verify-dashboard-after-image-diff-investigation.png`
- スクリーンショット: `output/playwright/verify-scan-after-image-diff-investigation.png`
- スクリーンショット: `output/playwright/verify-image-after-image-diff-investigation.png`
