日本語で回答する。

このリポジトリでは、Windows PowerShell 5.1 から直接実行される `.ps1` を扱う。

`.ps1` ファイルは ASCII のみにする。UTF-8 BOM は先頭に限り許容するが、日本語などの非 ASCII 文字列は PowerShell スクリプト内に入れない。

ユーザー向けの日本語文言は Blazor/C# UI、JSON リソース、Markdown ドキュメントに置く。PowerShell 補助サーバーや起動スクリプトのエラーメッセージは ASCII の英語にする。

PowerShell スクリプトを編集したら、ASCII のみであることを確認する。
確認方法を一時的にスクリプト化する場合は `scripts-temp/` 側で扱い、正式な検証手順として採用する場合だけ `scripts/` に置く。
`start.ps1` / `stop.ps1` を編集した場合は、少なくとも構文パースと非 ASCII 文字の有無を確認する。

Timeline 本体の通常起動・停止・状態確認は C# Launcher 経由で行う。`bat` / `sh` / `.command` をユーザー向け入口として追加しない。

`start.ps1` / `stop.ps1` は低レベル退避手段として扱う。通常導線、READMEの主導線、OS自動起動、画面上の復旧案内では前面に出さない。

Timeline から各サブ製品を操作する場合は、原則としてその製品のローカル API 経由で行う。サブ製品の起動・停止が必要な場合も、まず各製品が提供する正式なホスト側入口を確認する。

旧ホスト API ランチャーは廃止対象とする。Timeline 本体から通常操作として旧ホスト API ランチャーを直接呼ばない。

サブ製品の Docker コンテナへ直接入ってコマンドを実行しない。

## Jira とタスク管理

- Timeline の Jira プロジェクトは `KAN`。
- Jira サイトは `https://amanosystemlab.atlassian.net`。
- Atlassian Rovo で確認する場合の `cloudId` も `https://amanosystemlab.atlassian.net` を使う。
- Jira 一覧は https://amanosystemlab.atlassian.net/jira/software/projects/KAN/list?jql=project%20%3D%20KAN%20ORDER%20BY%20cf%5B10019%5D%20ASC を使う。
- Jira の運用ルールは `docs/jira-operation.md` を正本として参照する。
- Jira は実装計画と進捗の運用上の正本として扱う。
- GitHub はコードの正本として扱う。
- チケット状態、完了判定、PBI の有無確認は、ローカル資料だけで判断せず、必要に応じてクラウド Jira を直接確認する。
- ローカルの受入レポート、作業メモ、スクリーンショット、動画、ログは補助証跡として扱う。
- エピック、ストーリー/PBI、タスクは、原則として 2W1H で書く。
- ストーリー/PBI はユーザー価値を表すため、Who、What、Why を中心に書く。
- How が主語になるものは、原則としてストーリーではなくタスクに落とす。
- タスクは実装、運用、検証の作業単位として、What、Why、How を明確にする。
- すべてのストーリー/PBIには受入条件を書く。受入条件は、機能の有無ではなく、ユーザーの Why が満たされたかを確認できる形にする。
- 検証方法はチケットごとに明記する。コードテスト、画面レビュー、実データ確認、AI利用確認は別の検証手段として扱い、混同しない。
- Timeline では、単に画面やAPIができただけでは完了扱いにしない。素材が取り込まれ、時系列化され、必要に応じて概要やZIP出力を通じて AI が使える作業文脈になったかを重視する。
- AI が関わるストーリー/PBIでは、返答文や画面部品の存在だけを受入条件にしない。Timeline が保持する素材、概要、時系列イベント、出力ZIPが、ユーザーの次の判断やAI利用に役立つ状態になったかを確認する。
- ユーザーから割り込み作業、既存チケットにない作業、調査から見つかった追加作業を依頼された場合は、対応前または対応後の切りのよい時点で Jira チケット化する。
- 完了した作業は、対応するチケットの状態、説明、コメントが古いままにならないよう更新する。

## 一次ドキュメント・一時ドキュメント

- `docs/` は正式資料、または正式資料候補だけを置く場所として扱う。
- 調査メモ、移行メモ、レビュー結果、検証ログ、判断途中の資料は、ユーザーの明示的な合意なしに `docs/` へ置かない。
- 開発中にエージェントが作成する一時的な確認資料、設計メモ、検証ログ、判断材料は `docs-temp/` に置く。
- `docs-temp/` は Git 管理外の作業用資料として扱い、正式仕様や正本資料として扱わない。
- 正式な資料として残す価値がありそうなものは、ユーザーに確認し、同意を得てから `docs/` に移す。
- `docs/` に移した資料は正本候補として扱い、README、AGENTS.md、Jira の関連チケットから必要に応じて参照できるようにする。
- 既存のHTMLモックや途中検討資料は、正式仕様ではなく、明示的に採用された場合だけ正式資料へ昇格する。

## 一次スクリプト・一時スクリプト

- `scripts/` は正式な開発、検証、運用スクリプトの置き場として扱う。
- ただし、ローカル API や C# 側が責務を持つべき処理を、便宜的に `.ps1` として固定化しない。
- 画面やローカル操作から必要になる復旧、状態確認、Docker 制御は、できるだけローカル API または C# 側へ寄せる。
- Timeline 本体の通常起動・停止は C# Launcher を正本とする。`start.ps1` / `stop.ps1` は低レベル退避手段として扱い、`scripts/` 配下の一時補助スクリプトとは分けて考える。
- 単発確認、調査、移行確認、局所的なスモーク、検証補助のための一時スクリプトは `scripts-temp/` に置く。
- `scripts-temp/` は Git 管理外の作業用スクリプトとして扱い、正式な開発・運用手順として扱わない。
- `scripts-temp/` の中に正式手順として残す価値があるものが見つかった場合は、ユーザーに確認し、責務がローカル API / C# ではなくスクリプトでよい理由を明示してから `scripts/` に移す。
